# Security policy

Report vulnerabilities privately through GitHub's security advisory flow. Do not open public issues containing credentials, internal addresses, certificate material, or exploit details.

This plugin is read-only. It accepts only fixed HTTP(S) base URLs and health paths from validated site configuration, rejects embedded URL credentials and query strings, does not follow redirects, and reads optional authorization material from a deployment-supplied file.
